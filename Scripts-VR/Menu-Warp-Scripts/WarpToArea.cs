using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Warp to designated area
public class WarpToArea : MonoBehaviour
{
    private Vector3 warpLocation; // x, y, z coordinates

    // What if ships move? Need a dynamic way to reference area

    void Start()
    {
        
    }

    void Update()
    {
        // Take input on menu
        // Teleport to that location
        
    }
}
